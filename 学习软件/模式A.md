请按照以下规则生成题目（模式 A：严格基于材料出题）：

1. **提取阶段：**
   - 请从提供的材料中提取出**核心知识点**，这些知识点包括：
     - 重要的定义
     - 关键公式
     - 主要结论
     - 关键步骤
     - 对比和区别（如果有）
     - 示例和应用（如果有）
   - **调用 DeepSeek 大模型**：请在提取知识点时，调用 DeepSeek 大模型对材料进行分析并提取出相关的关键信息。确保提取的内容与材料一致，且不引入外部常识或额外的知识。
     - **调用时机**：在开始提取材料中的知识点后调用 DeepSeek。

2. **出题阶段：**
   - 根据提取出的知识点生成题目：
     - 题目类型可以是选择题、填空题等。
     - 每道题目的答案必须能直接在材料中找到支持，**不允许外部常识的补充**。
     - 选择题应有 4 个选项，其中只有一个正确答案，且正确答案必须直接源自提取的材料。
     - 对于填空题，答案必须可以从材料中直接定位。
   - **调用 DeepSeek 大模型**：在生成题目时，调用 DeepSeek 大模型来确保题目与提取的知识点一致，并生成合理的干扰项或选项。
     - **调用时机**：在根据提取出的知识点生成题目时调用 DeepSeek，确保每道题的正确答案与材料一致。

3. **质量控制：**
   - 每道题目的选项要简洁、明确、清晰、明了，不要出现冗长或无关的选项。
   - 题目解析应能够清晰地说明为什么选择某个答案，且答案必须基于材料中的信息。

4. **输出格式：**
   请返回每道题的详细结构，格式如下：
   {
     "type": "单选" | "填空",  
     "stem": "题干",  
     "options": ["选项 A", "选项 B", "选项 C", "选项 D"],  
     "answer": "正确选项",  
     "explanation": "解析",  
     "evidence": "根据原文的证据支持"
   }