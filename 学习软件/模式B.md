请按照以下规则生成题目（模式 B：允许补充常识，灵活出题）：

1. **提取阶段：**
   - 请从提供的材料中提取出**核心知识点**，这些知识点包括：
     - 重要的定义
     - 关键公式
     - 主要结论
     - 关键步骤
     - 对比和区别（如果有，可以显示）
     - 示例和应用（如果有，可以显示）
   - **调用 DeepSeek 大模型**：请在提取知识点时，调用 DeepSeek 大模型对材料进行分析并提取出相关的关键信息。同时，DeepSeek 可以根据外部知识库补充一些常识性知识。
     - **调用时机**：在开始提取材料中的知识点后调用 DeepSeek，用以提取核心知识点并在此基础上引入外部常识。

2. **出题阶段：**
   - 根据提取出的知识点和外部常识生成题目：
     - 题目类型可以是选择题、填空题等。
     - 对于选择题，选项可以引入外部常识性知识，但必须与材料中的核心概念一致，并且常识性知识与材料紧密关联。
     - 对于填空题，答案可以根据常识和材料中的内容共同推导出来，但常识部分必须能够与材料中的信息相符。
   - **调用 DeepSeek 大模型**：在生成题目时，调用 DeepSeek 大模型来生成合理的题目选项，并确保常识性知识的引入不会偏离材料的核心概念。
     - **调用时机**：在生成题目时调用 DeepSeek，确保生成的选项合理，并且符合材料中提取的知识点。

3. **质量控制：**
   - 请确保题目和选项有**逻辑一致性**，避免出现无关的或过于模糊的选项。
   - 请避免过度依赖常识性知识，确保材料中的知识点仍然是出题的核心。
   - 题目解析应该清晰地说明正确答案的依据，并指出常识性知识在选项生成中的作用。

4. **输出格式：**
   请返回每道题的详细结构，格式如下：
   {
     "type": "单选" | "填空",  
     "stem": "题干",  
     "options": ["选项 A", "选项 B", "选项 C", "选项 D"],  
     "answer": "正确选项",  
     "explanation": "解析",  
     "evidence": "根据材料和常识的支持"
   }